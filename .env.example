# Neo4j (disable auth for local dev, or set password)
NEO4J_PASSWORD=neo4j

# Redis (default localhost)
REDIS_HOST=localhost
REDIS_PORT=6379

# Ollama
OLLAMA_MODEL=qwen2.5:7b
EMBEDDING_MODEL=nomic-embed-text
